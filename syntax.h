/*#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "bt.h"
#include "syntax.h"
#include "SymbolT.h"*/

extern struct BinaryTree* initializeTree();
extern struct BTNode* createNewNode(char*);
extern char* getHex(struct BinaryTree*, char*, int);
extern int getModeIndex(char*);
extern char* upper(char*);
extern char* only(char*);